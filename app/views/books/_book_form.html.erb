
  <%= form_with(model: @book) do |form| %>
    <div class="row">
      <div class="col-md-6">

        <div class="form-group">
          <%= form.label :title %>
          <%= form.text_field :title, id: :book_title,class:"form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :subtitle %>
          <%= form.text_area :subtitle, id: :book_subtitle,class:"form-control" %>
        </div>
      </div>

      <div class="col-md-6">
        <div class="form-group">
          <%= form.label :body %>
          <%= form.text_area :body, id: :book_body ,class:"form-control", rows: 15%>
        </div>
        
      </div>
    </div> 
      <div class="col-md-12">
          <h2> Technologies utilisées </h2>
          <div class="form-group">
            <%= form.fields_for :technologies do |techn_form| %>
            
              <%= techn_form.label :name %>
              <%= techn_form.text_field :name ,class:"form-control"%>
            
            <% end %>
          </div>

          <div class="form-group">
            <%= form.submit "Publier", class:"btn btn-primary"%>
          </div>
      </div>
    <% end %>
